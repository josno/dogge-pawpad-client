import React from "react";
import styled from "styled-components";

const UpdateBar = (props) => {
	return <UpdateBarStyles>{props.children}</UpdateBarStyles>;
};

const UpdateBarStyles = styled.div``;

export default UpdateBar;
